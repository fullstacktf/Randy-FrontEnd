import React, { FC } from "react";
//import styled from "@emotion/styled";

export const Settings: FC = () => {
  return <h1>Hola, soy los ajustes <span role="img" aria-label="emoji">🔧</span> </h1>;
};
